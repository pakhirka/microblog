{% extends 'base.html.twig' %}

{% block body %}
  <h1>Post creation</h1>

  {{ form_start(form) }}
  {{ form_widget(form) }}
  <div class="form-group">
    <button type="submit" class="btn btn-primary">{{ 'form.create' | trans }}</button>
  </div>
  {{ form_end(form) }}

  <a class="btn btn-default" href="{{ path('admin_posts_index') }}">Back to the list</a>
{% endblock %}
